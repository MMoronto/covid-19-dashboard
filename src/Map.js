import React from 'react';
import { Map as LeafletMap, TileLayer } from "react-leaflet";

function Map() {
	return (
		<div className="map">
			<h1>
				I bees a map Son!!
			</h1>
		</div>
	);
}

export default Map;